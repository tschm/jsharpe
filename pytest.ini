[pytest]
# Show test logs on the console so the messages are visible
log_cli = true
# Use DEBUG to show detailed messages; adjust locally if needed
log_cli_level = DEBUG
log_cli_format = %(asctime)s %(levelname)s %(name)s: %(message)s
log_cli_date_format = %H:%M:%S
addopts = -ra
